#!/bin/bash

# Delete the failing cluster and its corrupted storage claims
kubectl delete cluster coder-db -n coder
kubectl delete pvc -l cnpg.io/cluster=coder-db -n coder

# Apply the updated ArgoCD manifest
kubectl apply -f ../argocd.yaml
