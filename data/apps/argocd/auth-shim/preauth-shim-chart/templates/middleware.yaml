apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: pre-auth-gate
  namespace: {{ .Values.namespace }}
spec:
  forwardAuth:
    address: {{ .Values.middleware.spec.forwardAuth.address }}
    trustForwardHeader: true
