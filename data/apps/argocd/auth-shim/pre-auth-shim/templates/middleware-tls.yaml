apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: transfer-tls
  namespace: {{ .Values.namespace }}
spec:
  passTLSClientCert:
    info:
      subject:
        commonName: true
        organization: true
        country: true
