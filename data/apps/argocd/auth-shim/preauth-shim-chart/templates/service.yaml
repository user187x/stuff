apiVersion: v1
kind: Service
metadata:
  name: pre-auth-shim-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: pre-auth-shim
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
