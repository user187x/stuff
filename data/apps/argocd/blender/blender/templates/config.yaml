apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "blender.fullname" . }}-config
  labels:
  {{- include "blender.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.config.storageRequest | quote }}
