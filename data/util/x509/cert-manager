#!/usr/bin/env bash

CM_VERSION="1.0.0"
CM_CONFIG_DIR="${CM_CONFIG_DIR:-${XDG_CONFIG_HOME:-$HOME/.config}/cert-manager}"
CM_CONFIG_FILE="$CM_CONFIG_DIR/config"
CM_INFO_DIR="$CM_CONFIG_DIR/entries"
CM_INSTALL_DIR="$HOME/.local/bin"
CM_PASS_PREFIX="${CM_PASS_PREFIX:-cert-manager}"
CM_RUNTIME_DIR="${XDG_RUNTIME_DIR:-/tmp}/cert-manager-$(id -u)"
CM_STORE_DIR="${PASSWORD_STORE_DIR:-$HOME/.password-store}"

# Runtime Configurable (persisted in $CM_CONFIG_FILE)
CM_DEFAULT_TIMEOUT="8h"
CM_DEFAULT_ENTRY=""
CM_PURGE_GPG_CACHE="yes"

if [[ -t 1 ]]; then
 COLOR_R=$'\e[31m' COLOR_G=$'\e[32m' COLOR_Y=$'\e[33m' COLOR_B=$'\e[34m'
 COLOR_C=$'\e[36m' COLOR_D=$'\e[2m' COLOR_BO=$'\e[1m' COLOR_N=$'\e[0m'
else
 COLOR_R="" COLOR_G="" COLOR_Y="" COLOR_B="" COLOR_C="" COLOR_D="" COLOR_BO="" COLOR_N=""
fi

function cm_install-system {

 local this_path script_name target_path rc_line ans
 this_path=$(realpath "${BASH_SOURCE[0]:-$0}" 2>/dev/null || readlink -f "${BASH_SOURCE[0]:-$0}")

 if [[ -z "$this_path" || ! -f "$this_path" ]]; then
  cm_err "Could not determine the source script path."
  return 1
 fi

 script_name=$(basename "$this_path")
 target_path="$CM_INSTALL_DIR/$script_name"
 rc_line="source \"$target_path\""

 cm_info "Installing '$script_name'..."

 if [[ ! -d "$CM_INSTALL_DIR" ]]; then
  mkdir -p "$CM_INSTALL_DIR" || {
   cm_err "Failed to create directory $CM_INSTALL_DIR."
   return 1
  }
 fi

 if [[ "$this_path" == "$target_path" ]]; then
  cm_ok "Already installed at $target_path."
 else
  cp "$this_path" "$target_path" || {
   cm_err "Failed to copy the script to $CM_INSTALL_DIR."
   return 1
  }
  chmod +x "$target_path"
  cm_ok "Installed '$script_name' to $target_path"
 fi

 if [[ ":$PATH:" != *":$CM_INSTALL_DIR:"* ]]; then
  cm_warn "$CM_INSTALL_DIR is not in your \$PATH. Add to ~/.bashrc:  export PATH=\"$CM_INSTALL_DIR:\$PATH\""
 fi

 # Sourcing is what makes `env`, `unenv` and tab-completion work in the current shell.
 if [[ -f "$HOME/.bashrc" ]] && grep -qF "$rc_line" "$HOME/.bashrc"; then
  cm_ok "~/.bashrc already sources $target_path"
 else
  read -rp "Add '$rc_line' to ~/.bashrc so it loads in every shell? [Y/n] " ans
  if [[ ! $ans =~ ^[Nn] ]]; then
   printf '\n# cert-manager (timed TLS client-cert unlock)\n%s\n' "$rc_line" >>"$HOME/.bashrc" &&
    cm_ok "Added to ~/.bashrc  (run:  source ~/.bashrc)"
  else
   cm_info "Skipped. You can run it as '$script_name <cmd>' or source it manually."
  fi
 fi
}

function cm_ok { printf '%s✔%s %s\n' "$COLOR_G" "$COLOR_N" "$*"; }
function cm_info { printf '%s•%s %s\n' "$COLOR_B" "$COLOR_N" "$*"; }
function cm_warn { printf '%s!%s %s\n' "$COLOR_Y" "$COLOR_N" "$*" >&2; }
function cm_err { printf '%s✘ %s%s\n' "$COLOR_R" "$*" "$COLOR_N" >&2; }

function cm_load_config {

 CM_DEFAULT_TIMEOUT="8h"
 CM_DEFAULT_ENTRY=""
 CM_PURGE_GPG_CACHE="yes"
 [[ -f "$CM_CONFIG_FILE" ]] || return 0
 local k v
 while IFS='=' read -r k v; do
  case "$k" in
   default_timeout) CM_DEFAULT_TIMEOUT="$v" ;;
   default_entry) CM_DEFAULT_ENTRY="$v" ;;
   purge_gpg_cache) CM_PURGE_GPG_CACHE="$v" ;;
  esac
 done <"$CM_CONFIG_FILE"
}

function cm_save_config {

 mkdir -p "$CM_CONFIG_DIR" && chmod 700 "$CM_CONFIG_DIR"
 printf 'default_timeout=%s\ndefault_entry=%s\npurge_gpg_cache=%s\n' \
  "$CM_DEFAULT_TIMEOUT" "$CM_DEFAULT_ENTRY" "$CM_PURGE_GPG_CACHE" >"$CM_CONFIG_FILE"
 chmod 600 "$CM_CONFIG_FILE"
}

function cm_info_file { printf '%s/%s.info' "$CM_INFO_DIR" "$1"; }

function cm_info_get {
 local f
 f=$(cm_info_file "$1")
 [[ -f $f ]] && grep -m1 "^$2=" "$f" | cut -d= -f2-
}

function cm_info_set {
 local f tmp
 f=$(cm_info_file "$1")
 mkdir -p "$CM_INFO_DIR"
 chmod 700 "$CM_INFO_DIR"
 tmp=$(mktemp) || return 1
 [[ -f $f ]] && grep -v "^$2=" "$f" >"$tmp"
 printf '%s=%s\n' "$2" "$3" >>"$tmp"
 mv "$tmp" "$f" && chmod 600 "$f"
}

function cm_entry_path { printf '%s/%s' "$CM_PASS_PREFIX" "$1"; }
function cm_entry_exists { [[ -f "$CM_STORE_DIR/$CM_PASS_PREFIX/$1.gpg" ]]; }
function cm_entries { ls "$CM_STORE_DIR/$CM_PASS_PREFIX" 2>/dev/null | sed -n 's/\.gpg$//p'; }
function cm_rt { printf '%s/%s.%s' "$CM_RUNTIME_DIR" "$1" "$2"; } # name kind
function cm_now { date +%s; }
function cm_fmt_ts { date -d "@$1" '+%Y-%m-%d %H:%M:%S %Z'; }
function cm_human_size { numfmt --to=iec-i --suffix=B "$1" 2>/dev/null || printf '%s B' "$1"; }

function cm_parse_duration {
 local s="${1,,}" total=0 n u
 [[ -z "$s" ]] && return 1
 if [[ "$s" =~ ^[0-9]+$ ]]; then
  echo $((s * 3600))
  return 0
 fi
 while [[ -n "$s" ]]; do
  [[ "$s" =~ ^([0-9]+)([dhms]) ]] || return 1
  n=${BASH_REMATCH[1]}
  u=${BASH_REMATCH[2]}
  case $u in
   d) total=$((total + n * 86400)) ;;
   h) total=$((total + n * 3600)) ;;
   m) total=$((total + n * 60)) ;;
   s) total=$((total + n)) ;;
  esac
  s=${s#"${BASH_REMATCH[0]}"}
 done
 ((total > 0)) || return 1
 echo "$total"
}

function cm_fmt_dur {
 local t=$1 d h m s out=""
 ((t < 0)) && t=0
 d=$((t / 86400))
 h=$((t % 86400 / 3600))
 m=$((t % 3600 / 60))
 s=$((t % 60))
 ((d)) && out+="${d}d "
 ((d || h)) && out+="${h}h "
 ((d || h || m)) && out+="${m}m "
 out+="${s}s"
 printf '%s' "$out"
}

function cm_fmt_clock {
 local t=$1
 ((t < 0)) && t=0
 printf '%02d:%02d:%02d' $((t / 3600)) $((t % 3600 / 60)) $((t % 60))
}

function cm_bar {
 local rem=$1 tot=$2 w=${3:-40} pct filled empty col
 ((tot <= 0)) && tot=1
 ((rem < 0)) && rem=0
 pct=$((rem * 100 / tot))
 ((pct > 100)) && pct=100
 filled=$((w * pct / 100))
 empty=$((w - filled))
 if ((pct > 50)); then
  col=${COLOR_G}
 elif ((pct > 20)); then
  col=${COLOR_Y}
 else col=${COLOR_R}; fi
 printf '%s[%s' "${COLOR_D}" "${COLOR_N}"
 printf '%s' "$col"
 printf '%*s' "$filled" '' | tr ' ' '█'
 printf '%s' "${COLOR_D}"
 printf '%*s' "$empty" '' | tr ' ' '░'
 printf '%s]%s %3d%%' "${COLOR_D}" "${COLOR_N}" "$pct"
}

function cm_ensure_deps {

 [[ -n "${CM_DEPS_OK:-}" ]] && return 0
 local missing=() sudo=""
 command -v pass >/dev/null 2>&1 || missing+=(pass)
 command -v gpg >/dev/null 2>&1 || missing+=(gnupg)
 command -v openssl >/dev/null 2>&1 || missing+=(openssl)
 command -v curl >/dev/null 2>&1 || missing+=(curl)

 if ((${#missing[@]})); then
  cm_warn "Missing packages: ${missing[*]} — installing with apt-get"
  if ! command -v apt-get >/dev/null 2>&1; then
   cm_err "apt-get not available. Please install manually: ${missing[*]}"
   return 1
  fi
  [[ $EUID -ne 0 ]] && sudo=sudo
  $sudo apt-get update -qq && $sudo apt-get install -y -qq "${missing[@]}" ||
   {
    cm_err "Package installation failed"
    return 1
   }
  cm_ok "Installed: ${missing[*]}"
 fi

 if ! gpg --list-secret-keys --with-colons 2>/dev/null | grep -q '^sec'; then
  cm_warn "No GPG secret key found — pass needs one to encrypt certificate entries."
  local ans gname gmail
  read -rp "Generate a GPG key now? [Y/n] " ans
  if [[ ! $ans =~ ^[Nn] ]]; then
   read -rp "  Real name : " gname
   read -rp "  Email     : " gmail
   gpg --quick-generate-key "$gname <$gmail>" default default 0 ||
    {
     cm_err "GPG key generation failed"
     return 1
    }
   cm_ok "GPG key created"
  else
   cm_err "Cannot continue without a GPG key"
   return 1
  fi
 fi

 if [[ ! -f "$CM_STORE_DIR/.gpg-id" ]]; then
  local fpr
  fpr=$(gpg --list-secret-keys --with-colons 2>/dev/null | awk -F: '/^fpr/{print $10; exit}')
  cm_info "Initialising password store with GPG key $fpr"
  pass init "$fpr" >/dev/null || {
   cm_err "pass init failed"
   return 1
  }
 fi

 mkdir -p "$CM_CONFIG_DIR" "$CM_INFO_DIR" "$CM_RUNTIME_DIR"
 chmod 700 "$CM_CONFIG_DIR" "$CM_INFO_DIR" "$CM_RUNTIME_DIR"
 CM_DEPS_OK=1
}

function cm_kill_timer {

 local pid
 pid=$(grep -m1 '^timer_pid=' "$(cm_rt "$1" meta)" 2>/dev/null | cut -d= -f2)
 [[ -n $pid ]] && kill "$pid" 2>/dev/null
 return 0
}

function cm_spawn_timer {

 local name=$1 secs=$2 info
 info=$(cm_info_file "$name")
 (
  nohup bash -c '
      sleep "$1"
      rm -f "$2/$3.pw" "$2/$3.pem" "$2/$3.meta"
      if [ -f "$4" ]; then
        grep -v "^last_locked=" "$4" > "$4.tmp" 2>/dev/null
        printf "last_locked=%s (auto)\n" "$(date -Is)" >> "$4.tmp"
        mv "$4.tmp" "$4"
      fi' _ "$secs" "$CM_RUNTIME_DIR" "$name" "$info" >/dev/null 2>&1 &
  echo $!
 )
}

function cm_wipe_runtime {

 local f
 for f in pw pem meta; do
  f=$(cm_rt "$1" "$f")
  [[ -f $f ]] && { command -v shred >/dev/null && shred -u "$f" 2>/dev/null || rm -f "$f"; }
 done
}

function cm_meta_get { grep -m1 "^$2=" "$(cm_rt "$1" meta)" 2>/dev/null | cut -d= -f2; }

function cm_is_unlocked {
 local name=$1 exp
 [[ -f "$(cm_rt "$name" meta)" && -f "$(cm_rt "$name" pw)" ]] || return 1
 exp=$(cm_meta_get "$name" expires_at)
 if [[ -z $exp ]] || (($(cm_now) >= exp)); then
  cm_kill_timer "$name"
  cm_wipe_runtime "$name"
  cm_info_set "$name" last_locked "$(date -Is) (auto)"
  return 1
 fi
 return 0
}

function cm_require_unlocked {
 cm_is_unlocked "$1" && return 0
 cm_err "'$1' is locked. Run:  cert-manager unlock $1 [duration]"
 return 1
}

function cm_add {

 local name=$1 pem=$2 pw pw2 created sha
 [[ -z $name || -z $pem ]] && {
  cm_err "usage: cert-manager add <name> <file.pem>"
  return 1
 }
 [[ $name =~ ^[A-Za-z0-9._-]+$ ]] || {
  cm_err "name may only contain letters, digits, . _ -"
  return 1
 }
 [[ -r $pem ]] || {
  cm_err "cannot read '$pem'"
  return 1
 }
 grep -q 'BEGIN CERTIFICATE' "$pem" || {
  cm_err "'$pem' contains no certificate"
  return 1
 }
 grep -q 'PRIVATE KEY' "$pem" || cm_warn "'$pem' contains no private key — curl will need a separate key"

 if cm_entry_exists "$name"; then
  local ans
  read -rp "Entry '$name' already exists. Overwrite? [y/N] " ans
  [[ $ans =~ ^[Yy] ]] || return 1
 fi

 read -rsp "Password for the private key in $pem: " pw
 echo
 read -rsp "Confirm password: " pw2
 echo
 [[ $pw == "$pw2" ]] || {
  cm_err "passwords do not match"
  return 1
 }

 if grep -q 'PRIVATE KEY' "$pem"; then
  if ! openssl pkey -in "$pem" -passin fd:3 -noout 3<<<"$pw" 2>/dev/null; then
   cm_err "that password does not unlock the private key in $pem"
   return 1
  fi
  cm_ok "Password verified against private key"
 fi

 created=$(date -Is)
 sha=$(sha256sum "$pem" | cut -d' ' -f1)
 {
  printf '%s\n' "$pw"
  printf 'created: %s\nsource: %s\nsha256: %s\n' "$created" "$(readlink -f "$pem")" "$sha"
  cat "$pem"
 } | pass insert -m -f "$(cm_entry_path "$name")" >/dev/null ||
  {
   cm_err "pass insert failed"
   unset pw pw2
   return 1
  }
 unset pw pw2

 local subj iss nb na algo bits enc cnt

 subj=$(openssl x509 -in "$pem" -noout -subject 2>/dev/null | sed 's/^subject= *//')
 iss=$(openssl x509 -in "$pem" -noout -issuer 2>/dev/null | sed 's/^issuer= *//')
 nb=$(openssl x509 -in "$pem" -noout -startdate 2>/dev/null | cut -d= -f2)
 na=$(openssl x509 -in "$pem" -noout -enddate 2>/dev/null | cut -d= -f2)
 algo=$(openssl x509 -in "$pem" -noout -text 2>/dev/null | awk -F': ' '/Public Key Algorithm/{print $2; exit}')
 bits=$(openssl x509 -in "$pem" -noout -text 2>/dev/null | grep -oE '\(([0-9]+) bit\)' | head -1 | tr -d '()')
 cnt=$(grep -c 'BEGIN CERTIFICATE' "$pem")

 if grep -q 'BEGIN ENCRYPTED PRIVATE KEY' "$pem"; then
  enc="PKCS#8 encrypted"
 elif grep -q 'Proc-Type: 4,ENCRYPTED' "$pem"; then
  enc="legacy PEM encrypted"
 elif grep -q 'PRIVATE KEY' "$pem"; then
  enc="UNENCRYPTED"
 else enc="none (no key)"; fi

 cm_info_set "$name" name "$name"
 cm_info_set "$name" created "$created"
 cm_info_set "$name" source "$(readlink -f "$pem")"
 cm_info_set "$name" size "$(stat -c %s "$pem")"
 cm_info_set "$name" sha256 "$sha"
 cm_info_set "$name" cert_count "$cnt"
 cm_info_set "$name" subject "$subj"
 cm_info_set "$name" issuer "$iss"
 cm_info_set "$name" not_before "$nb"
 cm_info_set "$name" not_after "$na"
 cm_info_set "$name" key_algo "${algo:-unknown}"
 cm_info_set "$name" key_bits "${bits:-?}"
 cm_info_set "$name" key_enc "$enc"

 if [[ -z $CM_DEFAULT_ENTRY ]]; then
  CM_DEFAULT_ENTRY=$name
  cm_save_config
 fi
 cm_ok "Stored certificate + password in pass entry $(cm_entry_path "$name")"
 [[ $enc == UNENCRYPTED ]] && cm_warn "Note: the private key in the PEM is not encrypted."
 cm_info "Unlock with:  cert-manager unlock $name [duration]   (default $CM_DEFAULT_TIMEOUT)"
}

function cm_unlock {
 local name=$1 dur=${2:-$CM_DEFAULT_TIMEOUT} secs content pw pem now exp tpid
 cm_entry_exists "$name" || {
  cm_err "no entry '$name' (see: cert-manager list)"
  return 1
 }
 secs=$(cm_parse_duration "$dur") || {
  cm_err "bad duration '$dur' (e.g. 8h, 90m, 1h30m, 2d)"
  return 1
 }

 content=$(pass show "$(cm_entry_path "$name")") ||
  {
   cm_err "could not decrypt entry (wrong GPG passphrase / cancelled)"
   return 1
  }
 pw=${content%%$'\n'*}
 pem=$(printf '%s\n' "$content" | sed -n '/^-----BEGIN/,$p')
 unset content

 cm_kill_timer "$name"
 mkdir -p "$CM_RUNTIME_DIR"
 chmod 700 "$CM_RUNTIME_DIR"
 printf '%s' "$pw" >"$(cm_rt "$name" pw)"
 chmod 600 "$(cm_rt "$name" pw)"
 printf '%s\n' "$pem" >"$(cm_rt "$name" pem)"
 chmod 600 "$(cm_rt "$name" pem)"
 unset pw pem

 now=$(cm_now)
 exp=$((now + secs))
 tpid=$(cm_spawn_timer "$name" "$secs")
 printf 'unlocked_at=%s\nexpires_at=%s\nduration=%s\ntimer_pid=%s\n' \
  "$now" "$exp" "$secs" "$tpid" >"$(cm_rt "$name" meta)"
 chmod 600 "$(cm_rt "$name" meta)"
 cm_info_set "$name" last_unlocked "$(date -Is)"

 cm_ok "Unlocked '$name' for $(cm_fmt_dur "$secs")  →  relocks at $(cm_fmt_ts "$exp")"
 cm_info "Use:  cert-manager curl <args>   |   cert-manager password   |   cert-manager env"
}

function cm_lock {
 local names=() n
 if [[ $1 == --all ]]; then mapfile -t names < <(cm_entries); else names=("$1"); fi
 for n in "${names[@]}"; do
  if [[ -f "$(cm_rt "$n" meta)" ]]; then
   cm_kill_timer "$n"
   cm_wipe_runtime "$n"
   cm_info_set "$n" last_locked "$(date -Is) (manual)"
   cm_ok "Locked '$n'"
  else
   cm_info "'$n' is already locked"
  fi
 done
 if [[ $CM_PURGE_GPG_CACHE == yes ]] && command -v gpg-connect-agent >/dev/null 2>&1; then
  gpg-connect-agent reloadagent /bye >/dev/null 2>&1 && cm_info "gpg-agent passphrase cache cleared"
 fi
}

function cm_extend {
 local name=$1 dur=$2 secs exp new_secs meta
 cm_require_unlocked "$name" || return 1
 secs=$(cm_parse_duration "${dur:-1h}") || {
  cm_err "bad duration '$dur'"
  return 1
 }
 exp=$(cm_meta_get "$name" expires_at)
 exp=$((exp + secs))
 new_secs=$((exp - $(cm_now)))
 cm_kill_timer "$name"
 meta=$(cm_rt "$name" meta)
 sed -i "s/^expires_at=.*/expires_at=$exp/; s/^timer_pid=.*/timer_pid=$(cm_spawn_timer "$name" "$new_secs")/" "$meta"
 cm_ok "Extended '$name' by $(cm_fmt_dur "$secs")  →  relocks at $(cm_fmt_ts "$exp")"
}

function cm_status {
 local name=$1 f state now unl exp dur rem
 cm_entry_exists "$name" || {
  cm_err "no entry '$name'"
  return 1
 }
 f=$(cm_info_file "$name")
 printf '\n%s%sCertificate entry: %s%s%s\n' "${COLOR_BO}" "${COLOR_C}" "$name" "${COLOR_N}" \
  "$([[ $name == "$CM_DEFAULT_ENTRY" ]] && printf '  %s(default)%s' "${COLOR_D}" "${COLOR_N}")"
 printf '%s────────────────────────────────────────────────────────%s\n' "${COLOR_D}" "${COLOR_N}"

 if cm_is_unlocked "$name"; then
  now=$(cm_now)
  unl=$(cm_meta_get "$name" unlocked_at)
  exp=$(cm_meta_get "$name" expires_at)
  dur=$(cm_meta_get "$name" duration)
  rem=$((exp - now))
  printf '  %-16s %sUNLOCKED%s\n' "State:" "${COLOR_G}${COLOR_BO}" "${COLOR_N}"
  printf '  %-16s %s\n' "Unlocked at:" "$(cm_fmt_ts "$unl")"
  printf '  %-16s %s\n' "Relocks at:" "$(cm_fmt_ts "$exp")"
  printf '  %-16s %s\n' "Unlock window:" "$(cm_fmt_dur "$dur")"
  printf '  %-16s %s%s%s   ' "Remaining:" "${COLOR_BO}" "$(cm_fmt_dur "$rem")" "${COLOR_N}"
  cm_bar "$rem" "$dur" 30
  printf '\n'
  printf '  %-16s %s\n' "Runtime files:" "$CM_RUNTIME_DIR/$name.{pw,pem}"
 else
  printf '  %-16s %sLOCKED%s\n' "State:" "${COLOR_R}${COLOR_BO}" "${COLOR_N}"
 fi
 printf '  %-16s %s\n' "Created:" "$(cm_info_get "$name" created)"
 printf '  %-16s %s\n' "Last unlocked:" "$(cm_info_get "$name" last_unlocked || true)"
 printf '  %-16s %s\n' "Last locked:" "$(cm_info_get "$name" last_locked || true)"
 printf '  %-16s %s\n' "pass entry:" "$(cm_entry_path "$name")  ($(cm_human_size "$(stat -c %s "$CM_STORE_DIR/$CM_PASS_PREFIX/$name.gpg")") gpg-encrypted)"

 printf '\n%s%sFile / certificate details%s\n' "${COLOR_BO}" "${COLOR_C}" "${COLOR_N}"
 printf '%s────────────────────────────────────────────────────────%s\n' "${COLOR_D}" "${COLOR_N}"
 local src size
 src=$(cm_info_get "$name" source)
 size=$(cm_info_get "$name" size)
 printf '  %-16s %s%s\n' "Source file:" "$src" "$([[ -e $src ]] || printf '  %s(no longer present)%s' "${COLOR_D}" "${COLOR_N}")"
 printf '  %-16s %s (%s bytes)\n' "PEM size:" "$(cm_human_size "$size")" "$size"
 printf '  %-16s %s\n' "SHA-256:" "$(cm_info_get "$name" sha256)"
 printf '  %-16s %s\n' "Certificates:" "$(cm_info_get "$name" cert_count) in chain"
 printf '  %-16s %s\n' "Subject:" "$(cm_info_get "$name" subject)"
 printf '  %-16s %s\n' "Issuer:" "$(cm_info_get "$name" issuer)"
 printf '  %-16s %s\n' "Valid from:" "$(cm_info_get "$name" not_before)"
 local na na_epoch days
 na=$(cm_info_get "$name" not_after)
 na_epoch=$(date -d "$na" +%s 2>/dev/null)
 if [[ -n $na_epoch ]]; then
  days=$(((na_epoch - $(cm_now)) / 86400))
  if ((days < 0)); then
   printf '  %-16s %s  %sEXPIRED%s\n' "Valid until:" "$na" "${COLOR_R}" "${COLOR_N}"
  elif ((days < 30)); then
   printf '  %-16s %s  %s(%d days left)%s\n' "Valid until:" "$na" "${COLOR_Y}" "$days" "${COLOR_N}"
  else printf '  %-16s %s  %s(%d days left)%s\n' "Valid until:" "$na" "${COLOR_D}" "$days" "${COLOR_N}"; fi
 else
  printf '  %-16s %s\n' "Valid until:" "$na"
 fi
 printf '  %-16s %s %s\n' "Key type:" "$(cm_info_get "$name" key_algo)" "$(cm_info_get "$name" key_bits)"
 printf '  %-16s %s\n' "Key encryption:" "$(cm_info_get "$name" key_enc)"
 printf '\n%sTip:%s cert-manager remaining %s   → live countdown\n\n' "${COLOR_D}" "${COLOR_N}" "$name"
}

function cm_countdown {

 local name=$1 exp dur rem now key width first=1 old_trap int=0
 cm_require_unlocked "$name" || return 1
 exp=$(cm_meta_get "$name" expires_at)
 dur=$(cm_meta_get "$name" duration)

 old_trap=$(trap -p INT)
 trap 'int=1' INT
 tput civis 2>/dev/null
 printf '%s%s⏱  %s%s  %s— relocks at %s   (q or Ctrl-C to quit)%s\n' \
  "${COLOR_BO}" "${COLOR_C}" "$name" "${COLOR_N}" "${COLOR_D}" "$(cm_fmt_ts "$exp")" "${COLOR_N}"

 while :; do
  now=$(cm_now)
  rem=$((exp - now))
  width=$(($(tput cols 2>/dev/null || echo 80) - 20))
  ((width > 60)) && width=60
  ((width < 10)) && width=10
  ((first)) || printf '\e[2A'
  first=0
  printf '\r\e[K   %s%s%s  remaining   %s(%s)%s\n' "${COLOR_BO}" "$(cm_fmt_clock "$rem")" "${COLOR_N}" "${COLOR_D}" "$(cm_fmt_dur "$rem")" "${COLOR_N}"
  printf '\r\e[K   '
  cm_bar "$rem" "$dur" "$width"
  printf '\n'
  if ((rem <= 0)) || [[ ! -f "$(cm_rt "$name" meta)" ]]; then
   printf '\n%s%s'\''%s'\'' has relocked.%s\n' "${COLOR_R}" "${COLOR_BO}" "$name" "${COLOR_N}"
   break
  fi
  key=""
  read -rsn1 -t 1 key
  ((int)) && break
  [[ $key == q || $key == Q ]] && break
  # expiry may move if `extend` runs elsewhere
  exp=$(cm_meta_get "$name" expires_at)
  dur=$(cm_meta_get "$name" duration)
  [[ -z $exp ]] && {
   printf '\n%s%s'\''%s'\'' has relocked.%s\n' "${COLOR_R}" "${COLOR_BO}" "$name" "${COLOR_N}"
   break
  }
 done
 tput cnorm 2>/dev/null
 if [[ -n $old_trap ]]; then eval "$old_trap"; else trap - INT; fi
 return 0
}

function cm_export {

 local name=$1 out="" cert_only=0 decrypt=0 content pem pw
 shift
 while (($#)); do
  case $1 in
   --cert-only) cert_only=1 ;;
   --decrypt-key) decrypt=1 ;;
   *) out=$1 ;;
  esac
  shift
 done
 cm_entry_exists "$name" || {
  cm_err "no entry '$name'"
  return 1
 }
 out=${out:-./$name.pem}

 if cm_is_unlocked "$name"; then
  pem=$(<"$(cm_rt "$name" pem)")
  pw=$(<"$(cm_rt "$name" pw)")
 else
  cm_info "'$name' is locked — decrypting via pass (GPG passphrase required)"
  content=$(pass show "$(cm_entry_path "$name")") || {
   cm_err "decrypt failed"
   return 1
  }
  pw=${content%%$'\n'*}
  pem=$(printf '%s\n' "$content" | sed -n '/^-----BEGIN/,$p')
  unset content
 fi

 [[ -e $out ]] && {
  local ans
  read -rp "'$out' exists. Overwrite? [y/N] " ans
  [[ $ans =~ ^[Yy] ]] || return 1
 }
 : >"$out" && chmod 600 "$out" || {
  cm_err "cannot write $out"
  return 1
 }

 if ((cert_only)); then
  printf '%s\n' "$pem" | sed -n '/BEGIN CERTIFICATE/,/END CERTIFICATE/p' >"$out"
  cm_ok "Exported certificate chain (no key) → $out"
 elif ((decrypt)); then
  cm_warn "Writing UNENCRYPTED private key to $out — protect this file!"
  printf '%s\n' "$pem" | sed -n '/BEGIN CERTIFICATE/,/END CERTIFICATE/p' >"$out"
  printf '%s\n' "$pem" | openssl pkey -passin fd:3 3<<<"$pw" >>"$out" ||
   {
    cm_err "key decryption failed"
    return 1
   }
  cm_ok "Exported certificate + decrypted key → $out"
 else
  printf '%s\n' "$pem" >"$out"
  cm_ok "Exported PEM (certificate + password-protected key) → $out"
 fi
 unset pem pw
}

cm_password() {
 local name=$1
 cm_require_unlocked "$name" || return 1
 if [[ $2 == --copy ]]; then
  if command -v wl-copy >/dev/null 2>&1; then
   wl-copy <"$(cm_rt "$name" pw)"
  elif command -v xclip >/dev/null 2>&1; then
   xclip -selection clipboard <"$(cm_rt "$name" pw)"
  else
   cm_err "no clipboard tool (wl-copy/xclip)"
   return 1
  fi
  cm_ok "Password copied to clipboard"
 else
  cat "$(cm_rt "$name" pw)"
  echo
 fi
}

cm_curl() {
 local name=$1
 shift
 cm_require_unlocked "$name" || return 1
 command curl --cert "$(cm_rt "$name" pem)" --key "$(cm_rt "$name" pem)" \
  --pass "$(<"$(cm_rt "$name" pw)")" "$@"
}

cm_env() {
 local name=$1
 cm_require_unlocked "$name" || return 1
 if [[ $2 == --print ]]; then
  printf 'export CM_CERT_FILE=%q\nexport CM_CERT_PASS=%q\n' "$(cm_rt "$name" pem)" "$(<"$(cm_rt "$name" pw)")"
  return 0
 fi
 export CM_CERT_FILE
 CM_CERT_FILE=$(cm_rt "$name" pem)
 export CM_CERT_PASS
 CM_CERT_PASS=$(<"$(cm_rt "$name" pw)")
 cm_ok "Exported CM_CERT_FILE and CM_CERT_PASS  →  curl --cert \"\$CM_CERT_FILE\" --pass \"\$CM_CERT_PASS\" ..."
}
cm_unenv() {
 unset CM_CERT_FILE CM_CERT_PASS
 cm_ok "Cleared CM_CERT_FILE / CM_CERT_PASS"
}

cm_list() {
 local n state rem all
 mapfile -t all < <(cm_entries)
 ((${#all[@]})) || {
  cm_info "no entries yet — add one:  cert-manager add <name> <file.pem>"
  return 0
 }
 printf '%s%-20s %-10s %-22s %s%s\n' "${COLOR_BO}" "NAME" "STATE" "REMAINING" "SUBJECT" "${COLOR_N}"
 for n in "${all[@]}"; do
  if cm_is_unlocked "$n"; then
   rem=$(($(cm_meta_get "$n" expires_at) - $(cm_now)))
   state="${COLOR_G}unlocked${COLOR_N}"
   rem=$(cm_fmt_dur "$rem")
  else
   state="${COLOR_R}locked  ${COLOR_N}"
   rem="-"
  fi
  printf '%-20s %b   %-22s %s%s\n' "$n$([[ $n == "$CM_DEFAULT_ENTRY" ]] && printf ' *')" "$state" "$rem" \
   "$(cm_info_get "$n" subject | cut -c1-50)" ""
 done
 printf '%s(* = default entry;  timeout default: %s)%s\n' "${COLOR_D}" "$CM_DEFAULT_TIMEOUT" "${COLOR_N}"
}

cm_remove() {
 local name=$1 ans
 cm_entry_exists "$name" || {
  cm_err "no entry '$name'"
  return 1
 }
 read -rp "Permanently delete pass entry '$name' and its metadata? [y/N] " ans
 [[ $ans =~ ^[Yy] ]] || return 1
 cm_kill_timer "$name"
 cm_wipe_runtime "$name"
 pass rm -f "$(cm_entry_path "$name")" >/dev/null && rm -f "$(cm_info_file "$name")"
 [[ $CM_DEFAULT_ENTRY == "$name" ]] && {
  CM_DEFAULT_ENTRY=""
  cm_save_config
 }
 cm_ok "Removed '$name'"
}

cm_help() {
 cat <<EOF
${COLOR_BO}}cert-manager${COLOR_N} v$CM_VERSION — timed unlock for password-protected TLS client certs

${COLOR_BO}}Usage:${COLOR_N}  cert-manager [-e <entry>] <command> [args]
  install                     install into \$HOME/.local/bin and (optionally) source from ~/.bashrc

${COLOR_BO}}Setup / entries${COLOR_N}
  setup                       ensure pass, gpg, openssl, curl are installed & initialised
  add <name> <file.pem>       store PEM (cert + encrypted key) AND its password in pass
  list                        list entries and their lock state
  use <name>                  set the default entry
  remove <name>               delete an entry
  timeout [duration]          show or set the default unlock window (default 8h)

${COLOR_BO}}Lock control${COLOR_N}
  unlock [name] [duration]    decrypt once (gpg), keep usable for <duration> (default: timeout)
  lock   [name|--all]         relock now (wipes runtime secrets, clears gpg-agent cache)
  extend [name] [duration]    push the relock time out (default +1h)
  status [name]               lock details (locked/unlocked, created, remaining, file & cert info)
  remaining [name]            live countdown clock with progress bar (q to quit)

${COLOR_BO}}Using the cert (while unlocked, no prompts)${COLOR_N}
  curl <curl args...>         curl with --cert/--key/--pass injected automatically
  password [--copy]           print (or copy) the key password
  env [--print]               export CM_CERT_FILE / CM_CERT_PASS into this shell
  unenv                       clear those variables
  export [name] [out] [--cert-only|--decrypt-key]   write the PEM to a file

${COLOR_BO}}Durations:${COLOR_N} 8h, 45m, 1h30m, 2d, 90s   (bare number = hours)
${COLOR_BO}}Env:${COLOR_N}       CM_ENTRY=<name> selects an entry; PASSWORD_STORE_DIR honoured

Examples:
  cert-manager add prod-client ~/certs/client.pem
  cert-manager unlock                       # 8h
  cert-manager unlock prod-client 30m
  cert-manager curl -s https://api.internal/health
  cert-manager remaining
  cert-manager lock
EOF
}

cert-manager() {
 local entry="" cmd
 cm_load_config
 while [[ $1 == -e || $1 == --entry ]]; do
  entry=$2
  shift 2
 done
 cmd=${1:-help}
 shift 2>/dev/null

 case $cmd in help | -h | --help)
  cm_help
  return 0
  ;;
 install)
  cm_install-system
  return $?
  ;;
 version | -v)
  echo "cert-manager $CM_VERSION"
  return 0
  ;;
 esac
 cm_ensure_deps || return 1

 case $cmd in
  setup)
   cm_ok "All dependencies present, pass store initialised. Ready."
   return 0
   ;;
  add)
   cm_add "$@"
   return $?
   ;;
  list | ls)
   cm_list
   return $?
   ;;
  use)
   [[ -z $1 ]] && {
    cm_err "usage: cert-manager use <name>"
    return 1
   }
   cm_entry_exists "$1" || {
    cm_err "no entry '$1'"
    return 1
   }
   CM_DEFAULT_ENTRY=$1
   cm_save_config
   cm_ok "Default entry → $1"
   return 0
   ;;
  remove | rm)
   cm_remove "$1"
   return $?
   ;;
  timeout)
   if [[ -z $1 ]]; then
    echo "default unlock window: $CM_DEFAULT_TIMEOUT ($(cm_fmt_dur "$(cm_parse_duration "$CM_DEFAULT_TIMEOUT")"))"
    return 0
   fi
   cm_parse_duration "$1" >/dev/null || {
    cm_err "bad duration '$1'"
    return 1
   }
   CM_DEFAULT_TIMEOUT=$1
   cm_save_config
   cm_ok "Default unlock window → $1"
   return 0
   ;;
  lock) if [[ $1 == --all ]]; then
   cm_lock --all
   return $?
  fi ;;
 esac

 local -a all
 if [[ -z $entry ]]; then
  if [[ -n ${CM_ENTRY:-} ]]; then
   entry=$CM_ENTRY
  elif [[ -n $1 ]] && cm_entry_exists "$1" && [[ $cmd != cm_curl ]]; then
   entry=$1
   shift
  elif [[ -n $CM_DEFAULT_ENTRY ]]; then
   entry=$CM_DEFAULT_ENTRY
  else
   mapfile -t all < <(cm_entries)
   ((${#all[@]} == 1)) && entry=${all[0]}
  fi
 fi
 if [[ -z $entry ]]; then
  cm_err "no entry selected. Add one (cert-manager add <name> <file.pem>) or pass -e <name>"
  return 1
 fi
 cm_entry_exists "$entry" || {
  cm_err "no entry '$entry' (cert-manager list)"
  return 1
 }

 case $cmd in
  unlock) cm_unlock "$entry" "$@" ;;
  lock) cm_lock "$entry" ;;
  extend) cm_extend "$entry" "$@" ;;
  status | show | info) cm_status "$entry" ;;
  remaining | time | countdown | watch) cm_countdown "$entry" ;;
  export) cm_export "$entry" "$@" ;;
  password | pass | pw) cm_password "$entry" "$@" ;;
  curl) cm_curl "$entry" "$@" ;;
  env) cm_env "$entry" "$@" ;;
  unenv) cm_unenv ;;
  *)
   cm_err "unknown command '$cmd'"
   cm_help
   return 1
   ;;
 esac
}

function cert-manager-complete {

 local cur=${COMP_WORDS[COMP_CWORD]} prev=${COMP_WORDS[COMP_CWORD - 1]}
 case $prev in
  cert-manager) COMPREPLY=($(compgen -W "install setup add list use remove timeout unlock lock extend status remaining export password curl env unenv help" -- "$cur")) ;;
  install | unlock | lock | extend | status | remaining | export | password | env | use | remove | -e) COMPREPLY=($(compgen -W "$(cm_entries) --all" -- "$cur")) ;;
  add) COMPREPLY=($(compgen -f -- "$cur")) ;;
 esac
}
complete -F cert-manager-complete cert-manager 2>/dev/null

if [[ "${BASH_SOURCE[0]}" == "$0" ]]; then
 cert-manager "$@"
 exit $?
fi
