1. Install Python
===================================================================
https://www.python.org/ftp/python/3.13.7/python-3.13.7-amd64.exe

2. Install Pytorch
===================================================================
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

3. Install diffusers, transformers, accelerate
===================================================================
pip install diffusers transformers accelerate

4 Request permission from model (if needed) 
Copy token from huggingface : xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

4. Run Code from Huggingface button 'Use This Model' -> 'Diffusers'
===================================================================
from diffusers import DiffusionPipeline
from huggingface_hub import login

login("xxxxxxxxxxxxxxxxxxxxxxxx")

pipe = DiffusionPipeline.from_pretrained("black-forest-labs/FLUX.1-dev")

prompt = "Astronaut in a jungle, cold color palette, muted colors, detailed, 8k"
image = pipe(prompt).images[0]
