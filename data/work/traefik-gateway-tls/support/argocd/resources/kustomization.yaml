apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Replaces standard-install_gateway-api-crds download script
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.0.0/standard-install.yaml
  - middleware-preauth.yaml
  - httproutes.yaml
  - eso-secrets.yaml
