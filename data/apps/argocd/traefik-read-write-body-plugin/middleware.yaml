apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: modify-page
  namespace: excalidraw
spec:
  plugin:
    rewrite-body:
      lastModified: true
      rewrites:
        - regex: '<button class="help-icon"[^>]*>[\s\S]*?<\/button>'
          replacement: ""
