
========================================================
File: README.md
========================================================
# Coder ApplicationSet — 4 environments

This drops into your existing `coder-umbrella` repo and replaces the single
`Application` with one `ApplicationSet` that produces:

| Environment | Cluster                | Branch source           | Namespace                          |
|-------------|------------------------|-------------------------|------------------------------------|
| **prod**    | prod cluster           | `main`                  | `coder`                            |
| **staging** | staging cluster        | `staging`               | `coder`                            |
| **dev**     | dev cluster            | `develop`               | `coder`                            |
| **preview** | dev cluster (shared)   | any branch `^preview.*` | `coder-preview-<branchNormalized>` |

## Repo layout

```
your-repo/
├── coder-umbrella/                      # the Helm chart (unchanged)
│   ├── Chart.yaml
│   ├── values.yaml                      # base values, applies to all envs
│   ├── templates/
│   └── ...
├── environments/
│   ├── prod/values.yaml                 # prod-specific overrides
│   ├── staging/values.yaml
│   ├── dev/values.yaml
│   └── preview/values.yaml              # base preview overrides (env injected per branch)
└── argocd/
    ├── applicationset.yaml              # ← the new manifest
    └── github-token-secret.example.yaml # SCM provider credentials
```

## How it works

The ApplicationSet has **two generators** sharing a single top-level template:

### Generator 1 — `list` (static envs)

Iterates a hardcoded list of `{env, cluster, targetRevision}` tuples. Each
element produces one `Application` named `coder-<env>` pointing at the
matching cluster, syncing the matching branch, and loading
`environments/<env>/values.yaml`.

### Generator 2 — `scmProvider` (preview envs)

Calls the GitHub API every 30 minutes (default) to enumerate branches in
your repo, filtered down to those matching `^preview.*`. Each matching
branch produces an `Application` named `coder-preview-<branchNormalized>`
deployed to the **dev cluster** in its own namespace `coder-preview-<branchNormalized>`,
with `CODER_ACCESS_URL` templated to `https://<branchNormalized>.preview.coder.example.com`.

When a preview branch is deleted, the corresponding Application is removed
on the next reconcile, and `prune: true` in the syncPolicy cleans up the
namespace + all resources.

## Multi-source apps

Each generated Application uses two sources:

1. The chart at `path: coder-umbrella` on the appropriate branch
2. A second source with `ref: values` so per-environment values files at
   `environments/<env>/values.yaml` can be referenced via `$values/...`

This is ArgoCD's recommended way to keep chart + values in the same repo
without smuggling `..` into `valueFiles`.

## Per-environment override pattern

`coder.coder.env` is a Helm array, and Helm **replaces** arrays rather than
merging them. So each per-env values file defines the **full** env list
(connection URL + OAuth toggle + ACCESS_URL + any extras). The repetition
is small (3-5 entries) and explicit.

For previews, `environments/preview/values.yaml` deliberately omits the env
list — the ApplicationSet's preview generator injects it via
`helm.valuesObject`, with the branch name templated into the URL. That's
how each preview gets its own hostname without committing per-branch files.

## Bootstrap

```bash
# 1. Register your clusters with ArgoCD (one-time per cluster):
argocd cluster add prod-context     --name prod
argocd cluster add staging-context  --name staging
argocd cluster add dev-context      --name dev

# 2. Create the GitHub token secret:
kubectl -n argocd create secret generic github-token \
  --from-literal=token=ghp_xxxxxxxxxxxxxxxxxxxx
kubectl -n argocd label secret github-token argocd.argoproj.io/secret-type=scm-creds

# 3. Apply the ApplicationSet:
kubectl apply -f argocd/applicationset.yaml

# 4. Watch it reconcile:
argocd appset get coder-stack
argocd app list -l 'argocd.argoproj.io/application-set-name=coder-stack'
```

You should see three apps appear immediately (prod / staging / dev), plus
one app for each preview branch on the next SCM poll (≤30 min, or restart
the applicationset-controller for an immediate poll).

## Common adjustments

**Different repo per env.** Change `repoURL` per generator template — the
list elements can carry `repoURL` as another field, and the template
references `{{ .repoURL }}`.

**Namespace-restricted clusters.** Replace `server: '{{ .cluster }}'` with
`name: '{{ .clusterName }}'` and reference clusters by their ArgoCD name
instead of API URL.

**Pull requests instead of branches for previews.** Swap `scmProvider` for
the `pullRequest` generator. You get `number`, `branch`, `head_sha`,
`labels` etc. as parameters. Useful if you only want previews for *open*
PRs (auto-cleaned on merge/close).

**Preserving preview namespaces on deletion.** Set
`spec.preservedFields.annotations` on the ApplicationSet, or remove
`prune: true` from the preview generator's sync policy. By default,
deleting a preview branch deletes everything — usually what you want.

**Throttling the SCM poll.** Add `requeueAfterSeconds: 300` to the
`scmProvider` block to poll every 5 min instead of 30.

## Caveats

- The `scmProvider.github` generator paginates over **all repositories** in
  the configured org. The `repositoryMatch` filter is applied client-side
  *after* pagination, so for large orgs this can be slow. If that's a
  concern, run a separate ApplicationSet per repo or migrate to the
  `pullRequest` generator (single-repo).
- ApplicationSet template substitution happens **before** Helm rendering.
  `valuesObject` strings are templated by Go (`{{ .branch }}` etc.); inside
  the chart, the Helm `{{ }}` runs separately. Don't mix the two contexts.
- Preview branches need wildcard DNS (`*.preview.coder.example.com`) and a
  wildcard TLS cert. Set those up via cert-manager + DNS01 challenge.

========================================================
File: argocd/github-token-secret.yaml
========================================================
apiVersion: v1
kind: Secret
metadata:
  name: github-token
  namespace: argocd
  labels:
    # Required so the ApplicationSet controller can read this secret.
    argocd.argoproj.io/secret-type: scm-creds
type: Opaque
stringData:
  token: REPLACE_ME

========================================================
File: argocd/applicationset.yaml
========================================================
# =============================================================================
# Coder stack ApplicationSet
#
# One ApplicationSet, two generators:
#   1. `list`        → static envs:  prod, staging, dev
#   2. `scmProvider` → preview envs: one Application per branch matching
#                      `^preview.*` (deployed to the dev cluster)
#
# Each generator carries its own `template` override that gets deep-merged with
# the top-level template. Shared bits (project, syncPolicy, ignoreDifferences)
# live in the top-level template.
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: coder-stack
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]

  generators:
    - list:
        elements:
          - env: prod
            cluster: https://prod.k8s.example.com      # CHANGE ME (or use cluster name)
            targetRevision: main
          - env: staging
            cluster: https://staging.k8s.example.com   # CHANGE ME
            targetRevision: staging
          - env: dev
            cluster: https://dev.k8s.example.com       # CHANGE ME
            targetRevision: develop
      template:
        metadata:
          name: 'coder-{{ .env }}'
          labels:
            coder.com/environment: '{{ .env }}'
        spec:
          sources:
            - repoURL: https://github.com/your-org/your-repo.git   # CHANGE ME
              targetRevision: '{{ .targetRevision }}'
              path: coder-umbrella
              helm:
                releaseName: coder
                valueFiles:
                  - values.yaml
                  - '$values/environments/{{ .env }}/values.yaml'
            - repoURL: https://github.com/your-org/your-repo.git   # CHANGE ME
              targetRevision: '{{ .targetRevision }}'
              ref: values
          destination:
            server: '{{ .cluster }}'
            namespace: coder

    - scmProvider:
        github:
          organization: your-org                       # CHANGE ME
          allBranches: true
          tokenRef:
            secretName: github-token                   # see argocd/github-token-secret.example.yaml
            key: token
        filters:
          # Both conditions inside one filter object are AND'd.
          - repositoryMatch: '^your-repo$'             # CHANGE ME — limit to one repo
            branchMatch: '^preview.*'                  # branches prefixed with "preview"
      template:
        metadata:
          # `branchNormalized` lowercases and replaces non-alphanumerics with `-`.
          # e.g. branch "preview/feat-X" → branchNormalized "preview-feat-x"
          name: 'coder-preview-{{ .branchNormalized }}'
          labels:
            coder.com/environment: preview
            coder.com/preview-branch: '{{ .branchNormalized }}'
            coder.com/preview-branch-raw: '{{ .branch }}'
        spec:
          sources:
            - repoURL: https://github.com/your-org/your-repo.git   # CHANGE ME
              targetRevision: '{{ .branch }}'
              path: coder-umbrella
              helm:
                releaseName: coder
                valueFiles:
                  - values.yaml
                  - '$values/environments/preview/values.yaml'
                # Branch-specific overrides. Helm replaces (does not merge)
                # arrays, so the full env list is redefined here.
                valuesObject:
                  coder:
                    coder:
                      env:
                        - name: CODER_PG_CONNECTION_URL
                          valueFrom:
                            secretKeyRef:
                              name: coder-pg-app
                              key: uri
                        - name: CODER_OAUTH2_GITHUB_DEFAULT_PROVIDER_ENABLE
                          value: "false"
                        - name: CODER_ACCESS_URL
                          value: 'https://{{ .branchNormalized }}.preview.coder.example.com'  # CHANGE ME
            - repoURL: https://github.com/your-org/your-repo.git   # CHANGE ME
              targetRevision: '{{ .branch }}'
              ref: values
          destination:
            # All previews → dev cluster.
            server: https://dev.k8s.example.com         # CHANGE ME
            namespace: 'coder-preview-{{ .branchNormalized }}'

  template:
    metadata:
      name: placeholder            # always overridden in per-generator templates
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true   # avoids "no matches for kind Cluster" on first CNPG sync
          - ApplyOutOfSyncOnly=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
      ignoreDifferences:
        # CNPG operator continuously updates Cluster.status — don't fight it.
        - group: postgresql.cnpg.io
          kind: Cluster
          jsonPointers:
            - /status
        # Operator owns its webhook caBundle.
        - group: admissionregistration.k8s.io
          kind: MutatingWebhookConfiguration
          jqPathExpressions:
            - '.webhooks[].clientConfig.caBundle'
        - group: admissionregistration.k8s.io
          kind: ValidatingWebhookConfiguration
          jqPathExpressions:
            - '.webhooks[].clientConfig.caBundle'

========================================================
File: environments/staging/values.yaml
========================================================
# =============================================================================
# environments/staging/values.yaml — overrides for STAGING
# =============================================================================

cloudnative-pg:
  enabled: false                     # operator installed cluster-wide

postgres:
  enabled: true
  name: coder-pg
  instances: 2                       # 1 primary + 1 replica
  database: coder
  owner: coder

  storage:
    size: 30Gi
    storageClass: gp3                # CHANGE ME

  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      memory: 2Gi

  postgresql:
    parameters:
      max_connections: "200"
      shared_buffers: "512MB"

  monitoring:
    enabled: true

coder:
  coder:
    env:
      - name: CODER_PG_CONNECTION_URL
        valueFrom:
          secretKeyRef:
            name: coder-pg-app
            key: uri
      - name: CODER_OAUTH2_GITHUB_DEFAULT_PROVIDER_ENABLE
        value: "false"
      - name: CODER_ACCESS_URL
        value: "https://staging.coder.example.com"      # CHANGE ME

    replicaCount: 2

    resources:
      requests:
        cpu: 500m
        memory: 512Mi
      limits:
        memory: 1Gi

    service:
      type: LoadBalancer
      annotations:
        service.beta.kubernetes.io/aws-load-balancer-type: "nlb"

========================================================
File: environments/dev/values.yaml
========================================================
# =============================================================================
# environments/dev/values.yaml — overrides for DEV
# =============================================================================

cloudnative-pg:
  enabled: false                     # operator installed cluster-wide

postgres:
  enabled: true
  name: coder-pg
  instances: 1
  database: coder
  owner: coder

  storage:
    size: 10Gi

  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      memory: 1Gi

coder:
  coder:
    env:
      - name: CODER_PG_CONNECTION_URL
        valueFrom:
          secretKeyRef:
            name: coder-pg-app
            key: uri
      # Dev deliberately leaves the built-in GitHub OAuth provider on for
      # easy login. Don't do this in prod.
      - name: CODER_OAUTH2_GITHUB_DEFAULT_PROVIDER_ENABLE
        value: "true"
      - name: CODER_ACCESS_URL
        value: "https://dev.coder.example.com"          # CHANGE ME

    replicaCount: 1

    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        memory: 1Gi

    service:
      type: LoadBalancer

========================================================
File: environments/prod/values.yaml
========================================================
# =============================================================================
# environments/prod/values.yaml — overrides for the PROD environment
# =============================================================================

# CloudNativePG operator: assume already installed cluster-wide in prod.
# Flip to true if the umbrella should install/manage the operator itself.
cloudnative-pg:
  enabled: false

postgres:
  enabled: true
  name: coder-pg
  instances: 3                       # 1 primary + 2 streaming replicas
  database: coder
  owner: coder

  storage:
    size: 100Gi
    storageClass: gp3                # CHANGE ME — fast SSD class

  resources:
    requests:
      cpu: "1"
      memory: 2Gi
    limits:
      memory: 4Gi

  postgresql:
    parameters:
      max_connections: "400"
      shared_buffers: "1GB"
      effective_cache_size: "3GB"

  monitoring:
    enabled: true

  # Backups to S3 — wire up an IRSA role or static creds in `backup-creds`.
  backup:
    barmanObjectStore:
      destinationPath: s3://my-bucket-prod/coder-pg     # CHANGE ME
      s3Credentials:
        accessKeyId:
          name: backup-creds
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: backup-creds
          key: SECRET_ACCESS_KEY
      wal:
        compression: gzip
    retentionPolicy: "30d"

coder:
  coder:
    # Helm replaces arrays — define the full env list here.
    env:
      - name: CODER_PG_CONNECTION_URL
        valueFrom:
          secretKeyRef:
            name: coder-pg-app
            key: uri
      - name: CODER_OAUTH2_GITHUB_DEFAULT_PROVIDER_ENABLE
        value: "false"
      - name: CODER_ACCESS_URL
        value: "https://coder.example.com"              # CHANGE ME
      - name: CODER_PROXY_TRUSTED_HEADERS
        value: X-Forwarded-For
      - name: CODER_PROXY_TRUSTED_ORIGINS
        value: 10.0.0.0/8                               # CHANGE ME — your LB CIDR

    replicaCount: 3                  # HA control plane

    resources:
      requests:
        cpu: "1"
        memory: 1Gi
      limits:
        memory: 2Gi

    service:
      type: LoadBalancer
      externalTrafficPolicy: Local
      sessionAffinity: None
      annotations:
        service.beta.kubernetes.io/aws-load-balancer-type: "nlb"

    tls:
      secretNames:
        - coder-tls                  # cert-manager / external-secrets should populate this

========================================================
File: environments/preview/values.yaml
========================================================
# =============================================================================
# environments/preview/values.yaml — overrides for PREVIEW branches
#
# IMPORTANT: do NOT define `coder.coder.env` here. The ApplicationSet's
# preview generator overrides it via `helm.valuesObject` to inject the
# branch-specific CODER_ACCESS_URL.
# =============================================================================

cloudnative-pg:
  enabled: false                     # operator installed cluster-wide on dev cluster

postgres:
  enabled: true
  name: coder-pg
  instances: 1
  database: coder
  owner: coder

  # Small + ephemeral. Previews are short-lived.
  storage:
    size: 5Gi

  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 512Mi

coder:
  coder:
    replicaCount: 1

    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        memory: 512Mi

    # ClusterIP + Ingress is the typical preview pattern: each preview gets
    # a wildcard hostname like <branch>.preview.coder.example.com via your
    # ingress controller. (Set up wildcard DNS + cert-manager separately.)
    service:
      type: ClusterIP

    ingress:
      enable: true
      className: nginx               # CHANGE ME — your ingress class
      # The hostname is templated per-branch by the ApplicationSet's
      # valuesObject (see argocd/applicationset.yaml).
      tls:
        - secretName: coder-preview-wildcard-tls

========================================================
Project Directory Structure
========================================================
../../projects/coder/install-coder-helm/umbrella-applicationSet
├── argocd
│   ├── applicationset.yaml
│   └── github-token-secret.yaml
├── environments
│   ├── dev
│   │   └── values.yaml
│   ├── preview
│   │   └── values.yaml
│   ├── prod
│   │   └── values.yaml
│   └── staging
│       └── values.yaml
└── README.md

7 directories, 7 files
