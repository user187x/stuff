apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pre-auth
  namespace: traefik
spec:
  forwardAuth:
    address: http://platform-auth-shim.traefik.svc.cluster.local:8080/verify
    authResponseHeaders:
      - X-Forwarded-User
      - X-Forwarded-Groups
